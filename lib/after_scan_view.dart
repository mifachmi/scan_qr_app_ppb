part of '../_import.dart';

class AfterScanView extends StatelessWidget {
  const AfterScanView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
